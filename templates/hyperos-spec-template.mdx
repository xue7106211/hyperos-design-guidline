---
title: 规范标题
description: 一句话说明规范对象和适用场景
---

# 规范标题

## 概述

「对象名称」是一种用于「用户任务」的「组件/模式」。它通过「关键能力」帮助用户在 HyperOS 中完成「目标」。

### 核心属性

- **样式（Style）**：通过颜色、尺寸、形状、材质或层级表现视觉含义。
- **内容（Content）**：通过文本、图标、容器、媒体或业务内容传达任务。
- **角色（Role）**：说明组件在系统中的行为角色，例如行为触发、信息承载、状态反馈、上下文切换或并行任务。

## 核心原则

- **识别性（Recognition）**：组件应易于识别，并与周围内容形成清晰区分。
- **可操作性（Operability）**：组件应支持触控、鼠标、键盘和辅助设备，并提供足够容错。
- **状态反馈（State Feedback）**：组件应在交互过程中提供稳定、可信赖的反馈。

## 层级与类型

| 类型 | 层级 / 关系 | 用途 | 典型场景 | 关键约束 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 结构与组成

| 元素 | 必需 | 说明 |
| --- | --- | --- |
| 容器 | 是 | 承载主体内容和交互热区。 |

## 交互与状态

| 状态 | 触发条件 | 视觉变化 | 行为规则 |
| --- | --- | --- | --- |
| 默认 | 初始可用 | 使用默认容器与文字层级 | 可点击或可聚焦 |

## 使用指南

### 应做（Do）

- 应在「场景」中使用「类型」，因为「原因」。

### 不应做（Don't）

- 不应在「场景」中使用「类型」，因为「风险」。

## 变体详解

### 变体名称

说明此变体解决什么问题，何时使用。

#### 常见使用场景

#### 布局

#### 内容规则

#### 约束

## 规格

### 色彩

| 属性 | Token / 规则 | 备注 |
| --- | --- | --- |
| 主文本 | `{color.text.primary}` | 待与 HyperOS token 对齐 |

### 布局

### 尺寸

### 间距

### 圆角

### 热区

## 内容与文案

## 动效规范

## 响应式变化规则

### 高度规则

### 宽度规则

### 位置规则

### 多项排布

## 多端设计指南

| 终端 | 规则 |
| --- | --- |
| 手机 |  |
| 折叠屏 |  |
| 平板 |  |

## 无障碍与可用性

## 边界场景

| 场景 | 处理规则 |
| --- | --- |
| 长文本 |  |
| 空状态 |  |
| 错误状态 |  |

## 实现说明

| 项目 | 说明 |
| --- | --- |
| 状态来源 | 由业务状态控制；组件不应自行推断冲突状态。 |

## QA 检查清单

- [ ] 默认、按下、禁用、选中等状态与规范一致。
- [ ] 长文本、空数据、异常数据不会破坏布局。
- [ ] 深色模式和浅色模式均满足可读性要求。

## 从 OS3 迁移

| OS3 规则 | HyperOS 处理 | 原因 |
| --- | --- | --- |
|  | 保留 / 调整 / 新增 / 废弃 |  |

## 待确认

## 更改日志

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| V1 | YYYY-MM-DD | 初版。 |
